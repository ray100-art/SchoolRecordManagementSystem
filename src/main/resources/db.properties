# ============================================================
#  School Management System - PostgreSQL Connection Settings
#  Edit these values to match your PostgreSQL server
# ============================================================

db.host=localhost
db.port=5432
db.name=school_management
db.username=postgres
# Leave blank and set DB_PASSWORD in the environment instead.
db.password=

# Connection pool settings
db.maxConnections=10
db.connectionTimeout=30000